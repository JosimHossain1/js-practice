var a = 10;
var a = 20;
console.log(a) // output : 20

//If we declare a variable with var. We can re-declare the variable & re-assign the value of variable

let b = 30;
b = 40;
console.log(b) // output : 40
// When you declare a variable with let. Then you can't re-declare a variable again with the same name. But we can re-assign.

const c = 50;

console.log(c)

// When you decalre a variable with const. then you can't re-declared a variable with the same name and you can't re-assign.
